# 🎯 État de travail — session courante

**Version active : Business**

Toutes les modifications doivent aller dans le plugin séparé :
```
alliance-groupe-theme/assets/downloads/ag-business-avocat/
```

## Versions verrouillées (ne pas toucher)

- 🔒 **Free** (`ag-starter-avocat/`) — cadena via `LOCKED.md` + `.LOCK.sha256`
- 🔒 **Premium** (`ag-premium-avocat/`) — cadena via `LOCKED.md` + `.LOCK.sha256`

## Règles strictes Business

- Préfixe CSS Business : `.ag-business-*` uniquement
- Body class active : `body.ag-business-active`
- Hooks Free utilisés comme points d'extension (`do_action(...)`, `apply_filters(...)`)
- Aucun override des classes `.ag-premium-*` — Business **ajoute**, ne modifie pas
- Aucun override des classes Free — Business **ajoute**, ne modifie pas
- Aucune modif Free/Premium sans préfixe `unlock-free:` / `unlock-premium:` dans le commit

## Points d'extension disponibles

### Hooks Free
- `do_action('ag_brand_fallback')` — header logo fallback
- `do_action('ag_after_domaines')` — après la grille Domaines
- `do_action('ag_after_maitre')` — après section Maître
- `do_action('ag_after_honoraires')` — après section Honoraires
- `do_action('ag_after_cabinet')` — après section Cabinet
- `do_action('ag_after_hero')` — après le hero
- `do_action('ag_inside_maitre_body')` — dans le body Maître
- `apply_filters('ag_domaine_bg_url', $url, $icon, $post_id)` — image fond cards Domaines

### Body classes utilisables (côté CSS Business)
- `body.ag-business-active` (à ajouter par le plugin Business)
- `body.ag-premium-active` (présent en Premium et Business)
- `body.ag-light` (mode jour)
- `body.ag-tier-business` (du theme via pro-features.php)

## Vérifier l'intégrité

```bash
bash scripts/check-all-locks.sh
```

## Pour changer de version

L'utilisateur doit explicitement dire :
- "on bosse sur Premium" → unlock + justification dans `ag-premium-avocat/LOCKED.md`
- "on bosse sur Free" → unlock + justification dans `ag-starter-avocat/LOCKED.md`

---

*Ce fichier est le point de reprise pour Claude. À mettre à jour quand l'utilisateur change de version active.*
